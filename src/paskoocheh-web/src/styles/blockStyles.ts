import { css } from "@emotion/react";

import { paragraphP1Regular } from "src/styles/typeStyles";
import colors from "src/values/colors";

export const blockLink = css(paragraphP1Regular, {
  color: colors.secondary500,
});
