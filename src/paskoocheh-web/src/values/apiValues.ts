/**
 * The "Unknown" tool type’s slug (the default value of
 * `ToolNode.primaryToolType` in the admin).
 *
 * @remarks
 * This must stay in sync with the admin (`/admin/preferences/tooltype/`)!
 */
export const unknownToolTypeSlug = "unknown";

export const apiQueryItemLimit = 100;

export const timeoutApi = 10000;

export const paskoochehAppSlug = "paskoocheh";
